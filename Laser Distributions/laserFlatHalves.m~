%%
% Returns flat laser distributions on each half.
%
function returnPower = laserFlatHalves(X,Y,args)  

	global trackingParams;
    
    leftP  = args(1);
    rightP = args(2);
    
    returnPower = (trackingParams.bodyX < 0).*leftP + (trackingParams.bodyX >= 0).*rightP;


